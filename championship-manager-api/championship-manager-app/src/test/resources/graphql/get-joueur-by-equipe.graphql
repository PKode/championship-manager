query  {
    joueursByEquipe(equipeId: $equipeId) {
        id
        nom
        prenom
        poste
        nationalite
        dateNaissance
        taille
        poids
        equipe {
            id
            nom
        }
    }
}
