mutation match($match: MatchDtoInput!){
  match(match: $match) {
    id
    date
    domicile {
      id
      nom
    }
    exterieur {
      id
      nom
    }
    butDomicile
    butExterieur
  }
}
