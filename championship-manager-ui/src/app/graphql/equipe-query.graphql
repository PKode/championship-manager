query equipes {
  equipes {
    id
    nom
    championnat {
      id
      nom
    }
  }
}

query equipesOfChampionnat($championnatId: Int!) {
  equipesOfChampionnat(championnatId: $championnatId) {
    id
    nom
  }
}
